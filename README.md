## ⚙️ Installation

Make sure docker daemon is running. To install/build the dependencies:

```bash
 make build-dependencies
```

## ⚡️ Running The API

```bash
 make run
```

## 🧪 Test handlers

```bash
 make test
```
